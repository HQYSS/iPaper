---
description: iPaper 核心开发规则 — 每个 agent 每次会话必须遵守
alwaysApply: true
---

# iPaper 核心开发规则

## 零、强制入口与完成定义

开始任务时必须先读仓库根目录 `AGENTS.md`。用户明确要求“修复”“修改”或“开始改”后，实现任务的完成状态包括源码、测试、目标环境部署/重启和原始工作流验证；不得等待用户再次说“验收通过”或“收尾”才执行交付。

只改源码并通过测试不能称为“已修复”。结束前必须按任务范围执行 `scripts/verify-delivery.sh`，只有 `DELIVERY_COMPLETE=yes` 才能报告完成。

## 一、行为准则

### 1.1 先讨论后动手

在用户明确说"开始改"之前，不得执行任何代码修改。流程：
1. 分析任务，汇总所有需要改动的内容（涉及哪些文件、改什么、为什么）
2. 向用户完整呈现方案，等待讨论和确认
3. 用户明确同意后，才开始执行修改

### 1.2 高风险操作必须确认

以下操作执行前必须告知用户并获得确认：
- **服务器操作**：修改 Nginx 配置、重启服务、安装系统级包、修改 Xray/VPN 相关配置
- **数据操作**：删除/覆盖用户数据（论文、对话历史、用户画像）
- **架构变更**：改变 API 接口格式、数据存储结构、部署方式
- **重要依赖变更**：引入新的重要依赖、升级可能有 breaking change 的依赖
- **危险 Git 操作**：force push、删除分支、修改已 push 的 commit

低风险操作（修改代码、读取配置、git add/commit/push）直接执行，不要问。用户验收通过或明确说"收尾"后，`git push` 和符合第三区决策表的 `./scripts/deploy.sh` 也属于低风险收尾动作，除非本文件明确列为高风险操作。

### 1.3 能自己做的就自己做

不要让用户手动执行你有能力完成的操作：
- 重启 dev server → 直接自己重启
- 安装 npm/pip 依赖 → 直接安装
- git push → 直接 push
- 部署到服务器 → 按下方部署决策规则执行

反面例子：说"请你运行 npm run dev"、"请你 git push"。

### 1.4 提问而非假设

需求有歧义时问用户，不要猜。特别是：功能有多种实现方式且各有取舍、不确定某个改动是否会影响现有功能。

### 1.5 调试先看日志

遇到"没输出"、"卡住"、"无响应"、"状态异常"、"切换后错乱"、"请求超时"这类问题时，**第一步必须先看运行日志和运行时证据，再看代码**。

排查顺序：
1. 先确认当前实际运行的是哪条启动链、哪个进程在提供服务
2. 先看日志/终端输出/网络请求，定位问题发生在前端、Electron、后端还是上游服务
3. 有了运行时证据后，再阅读代码分析原因

禁止只凭静态读代码就直接下结论；如果还没看日志，只能提出假设，不能当成结论汇报给用户。

### 1.6 先确认用户正在看哪个环境

遇到“改了但没生效”或用户通过截图反馈 UI 问题时，第一步必须从地址栏、Electron 运行方式或运行时证据确认目标环境：

- `http://127.0.0.1:5173/` / `localhost:5173`：本地 Vite 开发态
- Electron 窗口：本地 Electron + Vite 或 `frontend/dist`
- `https://59.110.154.252/ipaper/`：公网生产态
- `chrome://extensions`：用户日常 Chrome profile 里已加载的扩展运行态

不得用“本地文件已修改”或“本地 dev server 已生效”推断生产环境已更新。

---

## 二、改动验证——强制自测清单

改完代码后，**必须按以下清单逐项执行**，不得跳过。

### 后端改动

1. 重启后端（如果后端在跑）：关闭旧进程 → 重新启动 uvicorn
2. 用 curl 验证改动涉及的 API 端点，确认返回符合预期
3. 如果改了数据模型/存储格式，验证旧数据是否兼容（读一个已有的论文/会话试试）

### 前端改动

1. 确认前端 dev server 在跑（没跑就启动）
2. **默认使用 Playwright 跑浏览器自动化自测**（你完全可以自己跑通，不要让用户肉眼验证）：
   - 首次在 `/tmp/ipaper-test/` 下 `npm i playwright && npx playwright install chromium`（约 3 分钟，装好可复用）
   - `chromium.launch({ headless: true })` → 访问 `http://127.0.0.1:5173/`（localhost 免登录）
   - 模拟用户操作（click / type / waitForFunction），截图到 `/tmp/ipaper-test/shots/` 下，再用 Read 工具读截图验证关键 UI 状态
   - 抓 `console` / `response.status >= 400` 作为副断言
   - 详细步骤、模板脚本见 `docs/dev/开发规范.md` § 六、浏览器自动化自测（Playwright）
3. `browser-use` subagent 在本环境**经常拿不到浏览器工具**，不要依赖它；Simple Browser 是给用户看的 iframe，agent 无法自动驱动
4. 只有 Playwright 真的走不通（如需要登录、需要真实桌面环境）才退回"让用户开浏览器帮你看"，并明确告诉用户"以下无法自测"
5. 描述观察到的结果，带上截图路径

### 前后端都改了

先验证后端，再验证前端。

### 无法验证时

如果环境限制无法完成某项验证，**必须明确告知用户**：
> "以下已验证：XXX。以下无法验证（原因）：YYY，需要你帮忙确认。"

**绝对不允许**出现"修改完成，请验收"而没有任何自测记录的情况。

---

## 三、实现任务的交付决策（build + deploy）

用户明确授权修改后，**自行判断是否需要重 build Electron app、是否需要部署云端，并在同一任务中自动执行**。不要等到用户追问或再次验收，不要问用户"需要 build 吗"、"需要部署吗"，也不要因为通用 Git/部署保守规则而停下来等待确认。

本项目的优先级约定：
- 若本规则与通用 agent 规则冲突，以本规则为准；`git push`、`./scripts/deploy.sh` 是本项目验收收尾的一部分。
- 只要不涉及第 1.2 节列出的高风险操作，收尾阶段应按决策表自动 push/build/deploy。
- `./scripts/deploy.sh` 会按参数执行前端本地构建/上传或后端打包/重启，但不代替 Git commit/push；这些都已被本项目授权为常规交付动作。
- 仍需确认的情况只有：修改 Nginx/Xray/系统级服务配置、删除或覆盖用户数据、改变稳定 API/数据存储结构且存在部署顺序风险、重要依赖变更、危险 Git 操作（force push、删分支、改已 push commit）。

### 决策表

| 改了什么 | Electron 桌面端 iPaper.app | 云端 `59.110.154.252/ipaper` |
|---|---|---|
| 只改文档 | 不动 | 不部署 |
| 只改后端代码 + 功能 Web 端可见 | 不需要重 build（Electron 通过 HTTP 连本地后端，代码 pull 即可）| `./scripts/deploy.sh` |
| 只改后端代码 + 功能仅 Electron 使用 | 确认本地后端已重启拿到新代码 | 不部署 |
| 改了前端 + 功能 Web 端可见 | **`./scripts/deploy.sh` 会顺带跑 `cd frontend && npm run build`，本地 dist 同步更新，Electron 下次双击启动就是新版 ✅** | `./scripts/deploy.sh` |
| 改了前端 + 仅 Electron 使用 | 本地 `cd frontend && npm run build` | 不部署 |
| 改了 `electron/` 下的代码 / 启动脚本 | 重启 iPaper.app | 不部署 |
| 改了 Service Worker (`public/sw.js`) | build + 递增 `CACHE_NAME` 版本号 | 部署 |
| 改了 Nginx 配置 | 不动 | **先向用户确认**（高风险） |
| 改了 Sync API、`sync_service.py` 或 revision 逻辑 | **云端部署成功后完整重启 iPaper.app，并实际同步一次** | **先部署服务器端**；breaking format 才需额外确认 |

### 完成后告知

- 部署了云端：只有在部署脚本的公网 `release.json` 校验通过后，才能说 *“已部署到服务器，https://59.110.154.252/ipaper/ 已是新版。”*
- 只 build 没部署：*"本地 `frontend/dist/` 已重新构建，下次双击 iPaper.app 就是新版。"*
- 都不需要：简要说明理由（如"本次只改后端 + 你已重启"）
- Sync 改动：必须运行 `./scripts/verify-delivery.sh --sync`，并在 `DELIVERY_COMPLETE=yes` 后才能说已修复。

---

## 四、收尾 checklist

实现和运行态验证完成后，逐条核对（**必须实际打开文档确认**，不要凭记忆判断"已经更新过了"）：

1. 列出本次改动涉及的文件和模块
2. 对照下表更新对应文档：

| 改了什么 | 需要更新的文档 | 更新什么 |
|----------|---------------|----------|
| 功能完成/发现 bug/新增待办 | `当前状态与待办.md` | 对应分类加一行 |
| 技术栈、目录结构、数据格式、启动方式 | `00-项目概览.md` | 保持示例代码/配置与实际一致 |
| 前端组件、Store、样式、API 封装 | `01-前端.md` | 新组件加文档、改了 Props/功能要同步 |
| 后端服务、API 接口、数据模型、配置 | `02-后端.md` | 服务描述、接口表、模型字段要同步 |
| Electron、iPaper.app、部署流程 | `03-Electron与部署.md` | 启动流程、目录结构、网络连接要同步 |
| 用户画像系统、自进化 pipeline | `04-用户画像与自进化.md` | pipeline 步骤、API、配置要同步 |

3. Git commit（有意义的 message，概括做了什么和为什么）
4. 按第三区决策表执行必要的 push/build/deploy；不要把"是否推送/部署"作为问题抛回给用户，除非命中高风险操作

---

## 五、按需参考

涉及特定领域时，读取对应文档获取细节：
- 代码风格、双端开发规则、服务器操作规程 → `docs/dev/开发规范.md`
- 项目架构、目录结构、数据格式 → `docs/dev/00-项目概览.md`
- 前端组件详解 → `docs/dev/01-前端.md`
- 后端 API / 服务层 → `docs/dev/02-后端.md`
- Electron / 部署 → `docs/dev/03-Electron与部署.md`
- 用户画像 / 自进化 → `docs/dev/04-用户画像与自进化.md`
- 云端同步 / 离线 → `docs/dev/05-云端同步与离线.md`
- 项目状态、已知 bug、待办功能 → `docs/dev/当前状态与待办.md`
