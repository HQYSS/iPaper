# 当前状态与待办

## 已完成功能

### 核心功能

| 功能 | 状态 | 说明 |
|------|------|------|
| arXiv 论文下载 | ✅ 完成 | 支持 URL 和 ID 两种输入格式 |
| 论文库管理 | ✅ 完成 | 列表、添加、删除、选中 |
| PDF 阅读 | ✅ 完成 | 基于 @react-pdf-viewer |
| AI 对话 | ✅ 完成 | 基于 Gemini，流式响应，SSE，支持停止生成 |
| 多会话支持 | ✅ 完成 | 每篇论文可创建多个独立对话会话，支持新建/切换/删除，记忆上次活跃会话 |
| 对话历史持久化 | ✅ 完成 | 按论文/会话独立保存为 JSON，支持旧 `chat_history.json` 懒迁移 |
| 消息编辑与分支 | ✅ 完成 | 编辑已发送的用户消息并重发，保留旧分支可通过 `< 1/2 >` 导航器切换 |
| 大 PDF 自动转图片 | ✅ 完成 | PDF > 15MB 时自动逐页渲染为 JPEG（150 DPI）传给 LLM，解决超大 PDF 请求超时 |
| PDF 导出 | ✅ 完成 | 下载原始 PDF |
| 自动讲解 | ✅ 完成 | 论文首个会话为空时自动触发讲解，后续新建会话不触发 |
| Profile-aware 讲解 | ✅ 完成 | system prompt 注入用户画像，量身定制讲解方式 |

### 用户画像与自进化

| 功能 | 状态 | 说明 |
|------|------|------|
| 用户画像文档 | ✅ 完成 | `~/.ipaper/user_profile/profile.md`，对齐访谈产出 |
| 画像编译为 system prompt | ✅ 完成 | 去掉元信息后原样注入 |
| 多步分析 pipeline | ✅ 完成 | 信号提取 → 编辑规划 → 程序化执行 → 验证 |
| 分析模型独立配置 | ✅ 完成 | Claude Opus 4.6，与讲解模型分离 |
| 手动触发分析 | ✅ 完成 | ChatPanel 标题栏按钮，对话 ≥2 轮时可用 |
| 更新确认 UI | ✅ 完成 | 蓝色通知卡片，可展开查看信号和编辑详情 |
| 变更日志 | ✅ 完成 | 每次更新追加到 changelog.md，含信号和编辑明细 |

### PDF 阅读器

| 功能 | 状态 | 说明 |
|------|------|------|
| 目录导航 | ✅ 完成 | 左侧可收起的书签面板 |
| 搜索 | ✅ 完成 | Cmd+F 呼出，Enter/Shift+Enter 导航 |
| 缩放 | ✅ 完成 | 带淡入淡出的平滑缩放 |
| 缩放比例记忆 | ✅ 完成 | 用户通过底部 `+ / -` 调整的 PDF 缩放会记忆到 `localStorage` |
| 页码显示 | ✅ 完成 | 底部工具栏显示当前页/总页数 |
| 缩放后居中 | ✅ 完成 | 自动居中 + 淡入淡出消除抖动 |
| 切语言保持视图 | ✅ 完成 | 切换 `EN / 中文 / 双语` 时恢复当前缩放和纵向滚动位置 |
| PDF 实验性调光 | ✅ 完成 | 底部工具栏支持 `Overlay / Brightness` 两种压暗方案及多档位 |
| 现代化 UI | ✅ 完成 | 浮动圆角工具栏，非传统 toolbar |

### 交互功能

| 功能 | 状态 | 说明 |
|------|------|------|
| PDF 文字选中引用 | ✅ 完成 | 选中后浮现「引用到对话」按钮 |
| 对话文字选中引用 | ✅ 完成 | 对话区域同样支持选中引用 |
| 多段引用 | ✅ 完成 | 支持多次选中累积引用，结构化传递给后端（带来源标注），不再嵌入 raw markup |
| 引用后自动聚焦输入框 | ✅ 完成 | 无论从 PDF 还是对话区引用，都会自动聚焦到底部输入框并把光标放到末尾 |
| 引用文字白名单清理 | ✅ 完成 | 选中文字用 Unicode 白名单清理，解决 PDF/KaTeX 特殊字体的乱码 |
| 引用显示 | ✅ 完成 | 彩色左边框+图标区分来源，点击展开长文本 |
| 流式输出智能滚动 | ✅ 完成 | 用户在底部时自动跟随，上翻阅读时不强制拉回 |
| 数学公式渲染 | ✅ 完成 | 对话中 LaTeX 公式正确渲染（KaTeX）|
| API Key 配置 UI | ✅ 完成 | 侧边栏齿轮按钮 → Settings Modal，未配置时自动弹出；密码模式输入，OpenRouter key 获取链接 |
| LLM 调用错误提示 | ✅ 完成（待验收） | 后端按异常类型返回中文错误消息，前端消息列表底部显示红色错误提示条 |

### 前端布局

| 功能 | 状态 | 说明 |
|------|------|------|
| 左侧论文栏可折叠 | ✅ 完成 | PanelLeftClose/Open 按钮，200ms 动画，`Cmd+B` 快捷键；打开论文后自动收起，鼠标移到最左侧可临时展开 |
| 右侧讲解面板可折叠 | ✅ 完成 | 标题栏 PanelRightClose 按钮 + 边缘 PanelRightOpen 按钮，`Cmd+L` 快捷键 |
| 讲解面板可拖拽调整宽度 | ✅ 完成 | 初始 480px，最小 320px，最大容器宽度的 50% |
| 讲解面板宽度记忆 | ✅ 完成 | 拖拽后的右栏宽度按比例记忆到 `localStorage`，窗口变化时按比例恢复 |
| 外观主题切换 | ✅ 完成 | 设置弹窗支持浅色 / 深色 / 跟随系统，壳层组件同步适配 |
| 笔记本式讲解面板 | ✅ 完成 | 用户消息为问题块（蓝色竖线），AI 回答为全宽 markdown |
| @tailwindcss/typography | ✅ 完成 | prose 排版类，行高/间距/标题优化 |

### 基础设施

| 功能 | 状态 | 说明 |
|------|------|------|
| Electron 桌面应用 | ✅ 完成 | macOS 可用 |
| 一键启动器 | ✅ 完成 | iPaper.app 双击启动（AppleScript applet 包装 bash 脚本，兼容 macOS Sonoma） |
| 后端自动管理 | ✅ 完成 | Electron 启动/关闭时管理后端生命周期 |
| LLM 配置 API | ✅ 完成 | 支持运行时更新 API Key |
| OpenRouter API 接入 | ✅ 完成 | 从 xhub.chat 迁移到 OpenRouter，统一 API 入口 |
| Reasoning 保存 | ✅ 完成 | 流式对话中捕获 reasoning tokens 并存入对话历史，多轮对话传回模型保持推理连续性 |
| Git 版本管理 | ✅ 完成 | GitHub 私有仓库 (HQYSS/iPaper) |
| AWS 服务器部署 | ✅ 完成 | Nginx 反向代理 + systemd，`https://www.moshang.xyz/ipaper/` |
| HTTP Basic Auth | ✅ 完成 | Nginx 层面认证，保护公网访问 |
| **Cursor 集成模式** | ✅ 完成 | 前端在 Simple Browser 中运行，Cursor 对话框替代 AI 讲解 |

### Cursor 集成模式

| 功能 | 状态 | 说明 |
|------|------|------|
| 前端 Cursor 模式 | ✅ 完成 | `?cursor=1` 参数隐藏讲解面板，PDF 占全宽 |
| 论文目录 | ✅ 完成 | `papers/` 存储在项目内部，Cursor 可直接读写 |
| 用户画像 Rule | ✅ 完成 | `.cursor/rules/ipaper-profile.mdc`，自动注入画像到每次对话 |
| 讲解约定 Rule | ✅ 完成 | `.cursor/rules/ipaper-conventions.mdc`，定义论文讨论规范 |
| 论文讲解 Skill | ✅ 完成 | `.cursor/skills/paper-explain/`，结构化讲解工作流 |
| 画像自进化 Skill | ✅ 完成 | `.cursor/skills/profile-evolution/`，独立对话中执行四步 pipeline |
| LaTeX 翻译 Skill | ✅ 完成 | `.cursor/skills/latex-translate/`，下载源码→翻译→编译 |
| Transcript 同步 | ✅ 完成 | `scripts/sync_transcripts.py`，后处理对话记录按论文保存 |
| Cursor 模式启动脚本 | ✅ 完成 | `start-cursor-mode.sh`，一键启动后端+前端 |

---

## 已知问题 (Bugs)

### P0 - 影响核心体验

| 问题 | 描述 | 根因分析 |
|------|------|---------|
| ~~搜索跳转后不居中~~ | ✅ 已修复。跳转后淡出 → 轮询等待滚动停止且高亮元素可见 → 居中 → 淡入 | 根因：1) `querySelector` 找到虚拟滚动保留的旧高亮元素 2) `scrollIntoView` 居中到窗口而非 PDF 容器。修复：`querySelectorAll` 取可见区域内最近元素 + 手动 `scrollTop` |

### P1 - 可用但体验不佳

| 问题 | 描述 |
|------|------|
| ~~Electron DevTools 自动打开~~ | ✅ 已修复，移除 `openDevTools()` 自动调用，需要时用 `Cmd+Option+I` 手动打开 |
| arXiv 速率限制 | `arxiv_service.py` 中 `arxiv.Client()` 未配置 `delay_seconds` 和 `num_retries`，频繁添加论文可能失败 |
| ~~后端端口不一致~~ | ✅ 已修复，三处均已统一为 3000 |
| ~~`main.py` 路由冲突~~ | ✅ 已修复，删除 `main.py` 中重复的 `GET /api/config` 路由 |
| 服务器内存受限 | AWS 服务器仅 472MB 内存，无法在服务器上执行 `npm run build`，需本地构建后上传 |

---

## 待开发功能

### V1.1 - 近期迭代

| 功能 | 优先级 | 说明 |
|------|--------|------|
| ~~搜索跳转居中~~ | ~~高~~ | ✅ 已完成 |
| ~~LLM 调用错误提示~~ | ~~高~~ | ✅ 已完成（待验收），见交互功能 |
| 论文摘要生成 | 中 | 添加论文后自动生成中文摘要用于论文库缩略显示 |
| iPhone Safari 适配 | 中 | 当前三栏布局在手机上不可用，需响应式或简化布局 |
| 离线支持优化 | 中 | PDF 阅读已支持离线，但应优雅处理 LLM 不可用的情况 |

### V2 - 中期规划

| 功能 | 说明 |
|------|------|
| LaTeX 翻译 | 下载 LaTeX 源码，LLM agent 逐段翻译，xelatex 编译为中文 PDF |
| 中英文 PDF 切换 | 按钮切换英文/中文 PDF |
| 位置同步 | 切换语言时保持阅读位置（section 级别同步）|

### V3 - 长期规划

| 功能 | 说明 |
|------|------|
| 多用户支持 | 用户登录系统，每人独立画像 |
| 移动端适配 | 响应式布局适配平板和手机 |
| 批量导入 | 支持一次导入多篇论文 |

---

## 部署架构

```
用户浏览器（Mac Chrome / iPhone Safari）
        │
        ▼ HTTPS (port 443)
    ┌─────────┐
    │  Xray   │  ← TLS 终结，VPN 流量正常代理
    │         │     非 VPN 流量 fallback ↓
    └────┬────┘
         ▼ (port 8080, localhost)
    ┌─────────┐
    │  Nginx  │  ← /ipaper/ → 前端静态文件 (dist/)
    │         │     /ipaper/api/ → 反向代理到后端 (:3000)
    │         │     /disk/ → filebrowser
    │         │     /alist/ → alist
    │         │     全部带 Basic Auth
    └─────────┘
         ▼ (port 3000, localhost)
    ┌─────────┐
    │ FastAPI  │  ← systemd 管理 (ipaper-backend.service)
    │ Backend  │     venv 隔离 Python 依赖
    └─────────┘
```

### 部署操作速查

```bash
# 本地改代码后同步到服务器
git add -A && git commit -m "..." && git push
ssh aws "cd ~/iPaper && git pull"

# 如果改了前端代码，需要本地构建后上传
cd frontend && npm run build
cd .. && tar czf /tmp/dist.tar.gz -C frontend dist/
scp /tmp/dist.tar.gz aws:/tmp/
ssh aws "cd ~/iPaper/frontend && tar xzf /tmp/dist.tar.gz && rm /tmp/dist.tar.gz"

# 如果改了后端代码，重启后端
ssh aws "sudo systemctl restart ipaper-backend"

# 如果改了 nginx 配置
ssh aws "sudo /usr/sbin/nginx -t && sudo /usr/sbin/nginx -s reload"

# 查看后端日志
ssh aws "sudo journalctl -u ipaper-backend -f"
```

---

## 开发注意事项

### 前端修改须知

1. **@react-pdf-viewer 的虚拟滚动**：该库使用虚拟滚动渲染 PDF，很多常规的 CSS 布局技巧不适用。操作内部 DOM 时需通过 JS 查找实际的滚动容器
2. **插件实例化**：`searchPlugin()` 等工厂函数内部调用了 React hooks，它们本身就是 hooks，必须在组件顶层直接调用（不能包在 `useMemo`/`useState`/`useRef` 中）。每次渲染都会调用，但通过 React hooks 机制保持内部状态
3. **选中文字交互**：mouseup/mousedown 监听需限定在各自的容器 ref 内，否则 PDF 和 Chat 的引用按钮会互相干扰
4. **PDF Worker**：使用 CDN 地址 `https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js`，需确保版本与 pdfjs-dist 一致
5. **生产构建路径**：`vite.config.ts` 中 build 模式 `base: '/ipaper/'`，`api.ts` 中 API_BASE 基于 `import.meta.env.BASE_URL` 动态计算

### 后端修改须知

1. **LLM 客户端懒加载**：`LLMService.client` 属性检测 API Key 变化后会重建客户端
2. **API 提供商**：OpenRouter（`https://openrouter.ai/api/v1`），通过 OpenAI Python SDK 调用
3. **PDF 上传方式**：PDF ≤ 15MB 时通过 `file` content type 发送 base64 编码的 PDF；PDF > 15MB 时自动用 PyMuPDF 逐页渲染为 JPEG（150 DPI, quality 85），以多张 `image_url` 发送
4. **Reasoning tokens**：通过 `extra_body={"reasoning": {"effort": "medium"}}` 启用 Gemini 思考模式，reasoning 在流式过程中累积并存入 `ChatMessage.reasoning` 字段，下一轮对话时传回模型
5. **arXiv 速率限制**：频繁请求 arXiv API 会被限流（HTTP 429），建议添加延迟和重试
6. **SSE 流格式**：严格遵循 `data: {json}\n\n` 格式，前端按行解析
7. **画像分析使用独立模型**：`ProfileAnalysisConfig` 配置 `anthropic/claude-opus-4.6`（OpenRouter），与讲解模型分离
8. **LLM 错误日志**：`APIStatusError` 等异常会通过 `logger.error` 记录完整的上游错误信息（含 OpenRouter 返回的 message），前端也会显示具体原因而非泛化提示
9. **用户消息即时持久化**：`POST /chat/{id}/{sid}` 收到用户消息后立即保存到文件，不等 LLM 回复完成。即使 LLM 出错或用户停止生成，刷新后消息不丢失

### 服务器须知

1. **内存受限**：服务器仅 472MB，不能在服务器上执行 `npm run build`，必须本地构建
2. **Xray 不能动**：443 端口由 Xray 管理，Nginx 只监听 8080（localhost），改 Nginx 配置前必须 `nginx -t` 测试
3. **Python 虚拟环境**：后端依赖在 `/home/admin/iPaper/backend/venv/` 中，不污染系统 Python

---

## 文档索引

### 操作文档（每次会话必读必更新）

| 文件 | 内容 |
|------|------|
| [当前状态与待办.md](./当前状态与待办.md) | 本文件：已完成功能、已知问题、待开发功能、部署架构 |
| [开发规范.md](./开发规范.md) | Agent 开发规范、文档维护闭环、协作规则 |

### 参考文档（按需查阅）

| 文件 | 内容 |
|------|------|
| [00-项目概览.md](./00-项目概览.md) | 技术栈、架构、目录结构、数据格式、启动方式 |
| [01-前端.md](./01-前端.md) | 组件详解、状态管理、API 封装、样式系统 |
| [02-后端.md](./02-后端.md) | API 接口、服务层、配置管理、LLM 集成 |
| [03-Electron与部署.md](./03-Electron与部署.md) | Electron 配置、启动流程、一键启动器、打包 |
| [04-用户画像与自进化.md](./04-用户画像与自进化.md) | 用户画像系统、自进化 pipeline、API、部署 |
